apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gokul-infra-dev
  namespace: argocd
  labels:
    app: gokul-infra
    tenant: opsera
    environment: dev
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  
  source:
    repoURL: https://github.com/opsera-test-org/gokul-s-delight-ui.git
    targetRevision: main
    path: .opsera-gokul-infra/k8s/overlays/dev
  
  destination:
    name: opsera-usw2-np
    namespace: opsera-gokul-infra-dev
  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  
  revisionHistoryLimit: 10
  
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas
